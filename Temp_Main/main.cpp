#include <iostream>
#include <algorithm>
#include <stack>
#include <vector>

using std::cin;
using std::cout;
using std::ios_base;
using std::stack;
using std::string;
using std::vector;

int main()
{
    ios_base :: sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    int N;
    stack<int> S;
    //현재 값보다 크고 오른쪽에 가장 가까이 있는 것
    //배열로 하면 훨씬 더 쉬울 것 같다.
    
    cin >> N;
    for(int i = 0; i < N; i ++)
        
}

