#include <iostream>
#include <algorithm>
#include <vector>

using std::cin;
using std::cout;
using std::ios_base;
using std::vector;

int main()
{
    cin.tie(NULL);
    cout.tie(NULL);
    ios_base::sync_with_stdio(false);
    
    int N = 1, K = 1;
    vector<vector<int>> V;
    vector<int> V2;
    int RestNum = 1000000007;
    //ㄹㅇ
    
}
