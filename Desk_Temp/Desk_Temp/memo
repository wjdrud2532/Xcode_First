Á¤·Ä Ctrl + K > F

#include <iostream>

using std::cin;
using std::cout;
using std::ios_base;

int main()
{
    ios_base::sync_with_stdio(false);
    cout.tie(nullptr);
    cin.tie(nullptr);


    system("pause");
    return 0;
}